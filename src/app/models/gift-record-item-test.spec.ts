import { GiftRecordItemTest } from './gift-record-item-test';

describe('GiftRecordItemTest', () => {
  it('should create an instance', () => {
    expect(new GiftRecordItemTest()).toBeTruthy();
  });
});
